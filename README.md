# stax

Interactive project scaffolder for opinionated TypeScript starters.

## Install

```sh
npm install -g stax-cli
```

## Usage

```sh
stax new my-app
stax add prisma
stax list
```

`stax new` can generate React, Express, or fullstack npm-workspace starters with optional Docker, Prisma, and GitHub Actions CI files.
