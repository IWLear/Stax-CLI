import { Route, Routes } from 'react-router-dom';
import type { ProjectSummary } from '@{{packageName}}/shared';

const summary: ProjectSummary = {
  name: '{{projectTitle}}',
  template: 'fullstack',
  generatedAt: new Date().toISOString()
};

export function App() {
  return (
    <Routes>
      <Route
        path="/"
        element={
          <main className="min-h-screen bg-slate-50 px-6 py-12 text-slate-950">
            <section className="mx-auto grid max-w-5xl gap-8 md:grid-cols-[1.1fr_0.9fr]">
              <div>
                <p className="text-sm font-semibold uppercase tracking-wide text-emerald-700">
                  Workspace
                </p>
                <h1 className="mt-3 text-4xl font-bold tracking-tight sm:text-5xl">
                  {summary.name}
                </h1>
                <p className="mt-4 max-w-2xl text-lg leading-8 text-slate-700">
                  Bring decisions, customer signals, and launch work into one focused operating view.
                </p>
              </div>
              <div className="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
                <h2 className="text-base font-semibold">Shared status</h2>
                <dl className="mt-4 space-y-3 text-sm">
                  <div className="flex justify-between gap-4">
                    <dt className="text-slate-600">Template</dt>
                    <dd className="font-medium">{summary.template}</dd>
                  </div>
                  <div className="flex justify-between gap-4">
                    <dt className="text-slate-600">Generated</dt>
                    <dd className="font-medium">{new Date(summary.generatedAt).toLocaleDateString()}</dd>
                  </div>
                </dl>
              </div>
            </section>
          </main>
        }
      />
    </Routes>
  );
}
